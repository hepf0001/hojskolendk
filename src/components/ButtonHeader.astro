---
const { variant = "primary", size = "medium" } = Astro.props;

const buttonStyling = "rounded-[0.5rem] px-xs py-xxs text-neutral-50 font-lato shadow-drop25";

const variantClasses = {
  primary: "bg-blue-700 hover:bg-blue-900",
  secondary: "bg-orange-600 hover:bg-orange-700",
};

const sizeClasses = {
  large: "text-medium",
  medium: "text-small",
  small: "text-base",
  xsmall: "text-body",
};
---

<button class:list={[buttonStyling, variantClasses[variant], sizeClasses[size]]}><slot /></button>
